# Spec 010 — Geofencing tasks

- [ ] C1: geofence fields on delivery record + PM site-pick / default geocode.
- [ ] C4: ontology `Geofence` / `hasGeofence` / `geofenceTrigger` closed vocab; regenerate; gates green.
- [ ] C1: geolocation watch + haversine distance + entry/arrival emit + spam guard + offline buffer.
- [ ] C1: `/api/schedule/<id>/geofence` + persistence (spec 004 auth path).
- [ ] C3: arrival closes JIT delivery + on-hand refresh (spec 009).
- [ ] D1: device E2E (fence change, spam guard, offline buffer).