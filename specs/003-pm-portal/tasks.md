# Spec 003 — PM Portal: tasks

- [ ] Role-scope admin endpoints at server (with 004-auth).
- [ ] Driver identity key decision + alignment.
- [ ] `already_scheduled` contract fix.
- [ ] Audit `api()` calls vs contracts (F4 rows).
- [ ] Tests for schedule CRUD / view modal payload.