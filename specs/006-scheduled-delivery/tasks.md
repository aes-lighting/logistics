# Spec 006 — Scheduled Delivery: tasks

- [ ] Align file-serving (WS-4).
- [ ] Align `send_to_pm` route (F4).
- [ ] Fix `already_scheduled` (calendar upcoming).
- [ ] Driver identity key + matching.
- [ ] E2E test: ticket → pack → start → complete, + revise at every stage.
- [ ] Verify complete/revision emails + attachments.