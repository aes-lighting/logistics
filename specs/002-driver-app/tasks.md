# Spec 002 — Driver App: tasks

- [x] Fetch-path audit vs `contracts/api.md` (F4 rows).
- [x] Attach `Authorization: Bearer <email>` on all authenticated calls (global fetch wrapper; still unverified identity — 004-auth).
- [ ] `canvas.toBlob` fallback for offline/older browsers.
- [ ] Real-device PWA + camera + signature + geolocation validation.
- [x] Reconcile file-serving path (WS-4).
- [ ] Wizard end-to-end once server contract lands.