# Spec 005 — Inventory: tasks

- [x] Decision recorded (extend server → wizard contract).
- [x] `scan_page` / `confirm_job` / `pallet_photo` / `finalize` on server.
- [x] `GET /api/inventory/pms` + `GET /api/schedule/warehouse/ready_to_pack`.
- [x] PM directory memo live in confirm flow + auto-email.
- [ ] Fix daily report (F5).
- [ ] Split-location + mark-shipped tests.