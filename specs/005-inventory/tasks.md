# Spec 005 — Inventory: tasks

- [ ] Decision recorded (extend server → wizard contract).
- [ ] `scan_page` / `confirm_job` / `pallet_photo` / `finalize` on server.
- [ ] `GET /api/inventory/pms` + `GET /api/schedule/warehouse/ready_to_pack`.
- [ ] PM directory memo live in confirm flow + auto-email.
- [ ] Fix daily report (F5).
- [ ] Split-location + mark-shipped tests.